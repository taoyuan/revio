"use strict";

const chalk = require('chalk');

console.log('');
console.log('## Run `%s` (as root) to install configurations',
	chalk.blue.bold('sudo revio install'), chalk.blue('/etc'));
console.log('');
